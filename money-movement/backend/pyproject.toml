[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "money-movement-backend"
version = "0.1.0"
requires-python = ">=3.11"

[tool.setuptools]
packages = ["app", "app.models", "app.routes", "app.schemas", "app.services"]
